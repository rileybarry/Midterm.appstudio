/*
myAnimals = ['dog', 'cat', 'horse', 'meerkat'];

for (i = 0; i < 2; i ++) {
  newAnimal = prompt(`Please enter the name of an animal.`).toLowerCase()

  myAnimals.unshift(newAnimal);
  console.log(myAnimals)

  lastAnimal = myAnimals.slice(-1);
  console.log(`The last animal is a/an ${lastAnimal}`);
}
*/