/*
shoppingList = []

let shop = 1  

while (shop == 1 || shop == 2) {
  shop = prompt(`Enter '1' to see your shopping list or '2' to add two items to your list. Press 'Cancel' to exit the program.`)
  if (shop == 1) {
    let i = shoppingList.length
    shoppingList.sort()
    while (i > -1) {
      console.log(shoppingList[i])
      i = i - 1
    }
  } else if (shop == 2) {
    let item1 = prompt(`Enter the first item`)
    let item2 = prompt(`Enter the second item`)
    shoppingList.push(item1, item2)
  }
}
*/