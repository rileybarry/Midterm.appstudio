/*
function calcAvgSquare(x, y, z, a) {
  return ((x + y + z)/3) * (a ** 2);
}

function divideTwo(one) {
  return Math.pow(one,2)
}

let ask = prompt(`Enter '1' to calculate average or '2' for squared calculation`)

if (ask == 1) {
  num1 = Number(prompt(`Please enter your first number.`));
  num2 = Number(prompt(`Please enter your second number.`));
  num3 = Number(prompt(`Please enter your third number.`));
  num4 = Number(prompt(`Please enter your fourth number.`));

  alert(`The answer is ${calcAvgSquare(num1, num2, num3, num4)}.`);
} else if (ask == 2) {
  numSquare = Number(prompt(`Which number would you like to square?`))
  
  alert(`The answer is ${divideTwo(numSquare)}`)
}
*/